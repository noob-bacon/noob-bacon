local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "Leaked", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})

local Tab = Window:MakeTab({
	Name = "Trainee",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddLabel("Notes Stage")

Tab:AddButton({
	Name = "Welcome to the training session!",
	Callback = function()
      		        if setclipboard then
            setclipboard("Welcome to the training session! As your trainer, I will guide you through everything you need to do in order to rank up.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Before we begin",
	Callback = function()
      		        if setclipboard then
            setclipboard("Before we begin, you should be seated at all times. If you cannot see my chat, adjust your camera.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Unless instructed to do so",
	Callback = function()
      		        if setclipboard then
            setclipboard("Unless instructed to do so, leaving your seat will result in a warning.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "The first stage of training",
	Callback = function()
      		        if setclipboard then
            setclipboard("The first stage of training will be the notes section. Listen carefully as I explain the training procedure you will undergo during your time at Washiez.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "By passing THREE training sessions",
	Callback = function()
      		        if setclipboard then
            setclipboard("By passing THREE training sessions, you will receive the rank Head Operator.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Each training session you attend",
	Callback = function()
      		        if setclipboard then
            setclipboard("Each training session you attend, you will be promoted ONE rank.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "By passing this training session",
	Callback = function()
      		        if setclipboard then
            setclipboard("By passing this training session, you will be ranked to Junior Operator and can work at the car wash.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Continue attending training sessions",
	Callback = function()
      		        if setclipboard then
            setclipboard("Continue attending training sessions to work your way up the ranks.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Although, once you achieve",
	Callback = function()
      		        if setclipboard then
            setclipboard("Although, once you achieve the rank Head Operator, you will NOT be able to attend training sessions.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Instead, work hard",
	Callback = function()
      		        if setclipboard then
            setclipboard("Instead, work hard and play the game frequently for a chance at being promoted to Shift Leader. ") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Shift Leaders can use the moderation system",
	Callback = function()
      		        if setclipboard then
            setclipboard("Shift Leaders can use the moderation system to handle trollers at the car wash and help out at training sessions.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "In future trainings",
	Callback = function()
      		        if setclipboard then
            setclipboard("In future trainings, you will be given a detailed explanation of what it takes to become a Shift Leader.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "It’s also important to know!",
	Callback = function()
      		        if setclipboard then
            setclipboard("It’s also important to know that the ranks at Washiez are separated into categories.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Junior Operators, Senior Operators",
	Callback = function()
      		        if setclipboard then
            setclipboard("Junior Operators, Senior Operators, and Head Operators are in the Entry Team.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "Shift Leaders, Super-visors",
	Callback = function()
      		        if setclipboard then
            setclipboard("Shift Leaders, Super-visors, Assistant Managers, and General Managers are in the Super-vision Team.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

Tab:AddButton({
	Name = "I will explain more about the Super-vision Team",
	Callback = function()
      		        if setclipboard then
            setclipboard("I will explain more about the Super-vision Team later in the training and how they will be useful to you.") -- Set clipboard text
            print("Clipboard updated!")
        else
            warn("setclipboard function is not supported in this environment.")
        end
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]
