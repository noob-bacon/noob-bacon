-- Place this script in the button within the innermost Frame

-- Reference to the ScreenGui
local screenGui = script.Parent.Parent.Parent.Parent -- Assumes the button is inside two nested Frames, which are inside the ScreenGui

-- Function to destroy the ScreenGui when the button is clicked
local function onButtonClicked()
    screenGui:Destroy()
end

-- Connect the function to the button's MouseButton1Click event
script.Parent.MouseButton1Click:Connect(onButtonClicked)
